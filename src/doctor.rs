
#[derive(Serialize)]
pub struct Doctor {
    pub name: String,
    pub address: String,
    pub zip_code: String,
    pub city: String,
    pub phone: String,
    pub email: String,
    pub website: String
}
